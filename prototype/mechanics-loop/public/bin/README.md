Drop 1-2s opted-in clips here, then redeploy. The build picks up every .mp4.

  delay/
  residue/
  keeping/
  justdone/

Name them so they sort the way you want the montage to run, dark or still first. Optional Lines go in src/bin.ts under LINES, keyed by `/bin/{relation}/{file}.mp4`.
